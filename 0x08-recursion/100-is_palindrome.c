#include "main.h"
/**
 * is_palindrome - function that returns 1 if a string is a palindrome and 0
 * @s: input string
 * Return: lenth
 */
int is_palindrome(char *s)
{
	if (*s == '\0')
		return (0);
	else
		retun(1 + (1 + _strken_recursoin));

	return (0);
}
/**
 *_palindrome - cd unction that returns the natural square
 XXZ
 aked tiless[counter] = rev;
