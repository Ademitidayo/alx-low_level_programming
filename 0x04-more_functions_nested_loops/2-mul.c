#include "main.h"
#include <stdio.h>

/**
 * mul -  function that multiplies two integers.
 * @a: number to multiply by b
 * @b: number to multiply by a
 * Return: result of multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
